# AI Self-Healing Report (Powered by LM Studio 🚀)

> **Repo:** `Blog`
> **Generated:** Sun, 05 Apr 2026 00:57:43 GMT
> **Severity:** 🟠 `high`
> **Category:** `config`

---

## Error Summary
Dockerfile build failed due to checksum mismatches of critical files (.next, eslint.config.mjs, package.json) during the deployment process.

---

## Root Cause
The Dockerfile build process encountered checksum errors when attempting to copy `.next`, `eslint.config.mjs`, `package.json` and related configuration files from the source repository. This indicates that either these files were missing from the deployed branch (`main`), had been modified, or the Docker build cache was corrupted. The Git branch may have diverged since the last successful build, leading to discrepancies.

---

## Fix Steps
1. Verify the `main` branch of the repository contains all necessary files and directories (specifically `.next`, `eslint.config.mjs`, `package.json`, `tsconfig.json`).
2. Commit and push any changes made to these files since the last successful build.

---

## Suggested Commands
```bash
docker builder prune
```
```bash
git pull origin main
```

---

## Raw Logs
<details>
<summary>Click to expand (truncated)</summary>

```
docker compose exited with a non-zero code. See error.md for details.
```
</details>

---
*Auto-generated by the Overdrive AI Self-Healing Agent. Review carefully before merging.*
